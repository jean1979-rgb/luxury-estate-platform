type Props = {
  value: number;
};

export default function LuxuryScore({ value }: Props) {
  return (
    <div className="rounded-[20px] border border-white/10 bg-[#121212] p-6 text-center">
      <p className="text-[10px] uppercase tracking-[0.35em] text-[#b8afa3]">
        Luxury Score
      </p>

      <div className="mt-4 text-4xl font-light text-[#d6c3a1]">
        {value}
      </div>

      <div className="mt-2 text-xs uppercase tracking-[0.3em] text-[#b8afa3]">
        curated index
      </div>
    </div>
  );
}
