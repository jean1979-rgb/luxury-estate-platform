type Props = {
  title: string;
};

export default function ContactCTA({ title }: Props) {
  return (
    <section className="rounded-[28px] border border-white/10 bg-[#121212] p-8 md:p-10">
      <p className="text-[10px] uppercase tracking-[0.35em] text-[#d6c3a1]">
        Private Access
      </p>

      <div className="mt-4 grid gap-6 md:grid-cols-12 md:items-center">
        <div className="md:col-span-7">
          <h3 className="text-3xl font-light md:text-4xl">
            Solicita acceso privado a {title}
          </h3>
          <p className="mt-3 max-w-2xl text-sm leading-7 text-[#b8afa3] md:text-base">
            Agenda una visita, solicita brochure o recibe atención personalizada
            para esta propiedad curada.
          </p>
        </div>

        <div className="md:col-span-5 md:flex md:justify-end">
          <a
            href="https://wa.me/"
            className="inline-flex items-center justify-center rounded-full border border-[#d6c3a1] px-6 py-3 text-xs uppercase tracking-[0.28em] text-[#f5f1eb] transition hover:bg-[#d6c3a1] hover:text-black"
          >
            Contactar ahora
          </a>
        </div>
      </div>
    </section>
  );
}
